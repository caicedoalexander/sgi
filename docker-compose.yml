services:
  app:
    build: .
    container_name: sgi-app
    restart: unless-stopped
    env_file: .env
    ports:
      - "80:80"
    volumes:
      - app_files:/var/www/html/webroot/files
      - app_uploads:/var/www/html/webroot/uploads

volumes:
  app_files:
  app_uploads:
